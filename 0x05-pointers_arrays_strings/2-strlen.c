#include "main.h"
#include <string.h>

/**
 * _strlen - return the length of string
 * @s: input string
 *
 * Return: number as length
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
